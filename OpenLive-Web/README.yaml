Meta:
  name: Open Live for Web
  # en or cn
  lang: en
  # | 代表这里是字符串
  description: |
    This tutorial shows you how to quickly create a live app using Agora Web SDK.

Prerequisites:
  - Node.js LTS

QuickStart:
  description:
    This section shows you how to prepare, build, and run the sample application.
  sections:
    - title: Obtain an App ID
      content: |
        $_{APPID}
        5. Rename **.env.example** to **.env** file. In this file, replace `<#YOUR Agora.io APP ID#>` with the App ID, and assign the token variable with the temp Access Token generated from dashboard.

            ```bash
            REACT_APP_AGORA_APP_ID=<#YOUR Agora.io APP ID#>
            REACT_APP_AGORA_LOG=true
            ```
    - title: Install dependencies and integrate the Agora Video SDK
      content: |
        
        $_{WEB_RUN}

Resources:
  document: https://docs.agora.io/en/
  issue: https://github.com/AgoraIO/Basic-Video-Broadcasting/issues

License:
  type: MIT